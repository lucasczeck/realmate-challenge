[project]
name = "realmate-challenge"
version = "0.1.0"
description = ""
authors = [{ name = "Seu nome aqui", email = "seu.email@email.com.br" }]
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["django (>=5.1.6,<6.0.0)", "djangorestframework (>=3.16.1,<4.0.0)", "django-user-agents (>=0.4.0,<0.5.0)", "python-dotenv (>=1.2.1,<2.0.0)", "psycopg (>=3.3.2,<4.0.0)"]

[tool.poetry.dependencies]
python = "^3.13"
django = ">=5.1.6,<6.0.0"
djangorestframework = ">=3.15.0,<4.0.0"
psycopg2-binary = ">=2.9.0,<3.0.0"
python-decouple = ">=3.8,<4.0.0"
django-cors-headers = ">=4.3.0,<5.0.0"
drf-spectacular = "^0.28.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"
pytest-django = "^4.8.0"
pytest-cov = "^5.0.0"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
